import React from 'react';
import { render, screen } from '@testing-library/react';
import ProtocolExpsitionAndResponsePrevention from './ProtocolExpositionAndResponsePrevention'

describe('The SupportSwitch', () => {
    it("renders", () => {
      expect(2).toBe(2)
    });
  });